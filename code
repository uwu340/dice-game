 patch-2
 #include <stdio.h>
 #include <stdio.h>

  #include <stdio.h>
  #include <conio.h>

main
 int main()  {

	int rollADie, rollADie2, totalDice, key = 0, key2 = 0, end = 0;
 patch-1
	srand(time(NULL));  // call thiss only once – seeds the random generators
	srand(time(NULL));  // call thiss only once – seeds the random generators

patch-2
	srand(time(NULL));  // call thiss only one – seeds the random generators

	srand(time(NULL));  // call thisss only once – seeds the random generators
 main
 main

	do
	{
		rollADie1 = rand() % 6 + 1;  // in the range from 1 - 6
		rollADie2 = rand() % 6 + 1;  // in  range of 1 - 6

		printf("Dice #1 is %i\n", rollADie1);
		printf("Dice #2 is %i\n", rollADie2);
		printf("Dice #2 is %i\n", rollADie2);

		totalDice = rollADie1 + rollADie2;
 patch-4
		printf("The total of both dice is %i\n", totalDice);
		printf("The total of both dice is %i\n", totalDice);

		printf("The total no of both dice is %i\n", totalDice);
 main
		if (totaldicev == 3){
			end = 3;
			break;}

                if(total dice == 2){
		   end = 2;
			break;patch-2
		}
		switch (totalDice)  {
		
		case 3:
			end = 3;
			break;
		main
		case 4:
			key = 4;
			break;
		case 5:
			key = 5;
			break;
		case 6:
			key = 6;
			break;
		case 7:
			end = 7;
			break;
		case 8:
			key = 8;
			break;
		case 9:
			key = 9;
			break;
		case 10:
			key = 10;
			break;
		case 11:
			end = 11;
			break;
		case 12:
			end = 12;
			break;
		}
	} while (totalDice != key);

	do
	{
		rollADie1 = rand() % 6 + 1;  // in the range of 1 - 6
		rollADie2 = rand() % 6 + 1;  // in the range of 1 - 6

		printf("Dice #1 is %i\n", rollADie1);
		printf("Dice #2 is %i\n", rollADie2);

		totalDice = rollADie1 + rollADie2;
		printf("The total of both dice is %i\n", totalDice);

		switch (totalDice)  {
		case 2:
			printf("Roll Again\n");
			key2 = 2;
			break;
		case 3:
			printf("Roll Again\n");
			key2 = 3;
			break;
		case 4:
			key = 4;
			break;
		case 5:
			key = 5;
			break;
		case 6:
			key = 6;
			break;
		case 7:
			printf("You Win\n");
			break;
		case 8:
			key = 8;
			break;
		case 9:
			key = 9;
			break;
		case 10:
			key = 10;
			break;
		case 11:
			printf("Roll Again\n");
			key2 = 11;
			break;
		case 12:
			printf("Roll Again\n");
			key2 = 12;
			break;
		}
	} while (totalDice == key2);

	if (totalDice == 7) {
		printf("You are the Winner\n");
	}
	else
	{
		if (totalDice == key);
		printf("You are a Loser\n");
	}


	printf("Thank you for Playing\n");

	system("pause");
	return 0;
}
